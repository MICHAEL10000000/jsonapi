{
  "name": "jsonapi",
  "version": "1.0.0",
  "description": "To test api deployment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --eatch db.json --port 3000"
  },
  "author": "Gbenga",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  }
}
